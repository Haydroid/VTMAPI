<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bonc">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/renling"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".driver.instruction.TestMainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".f6test.DeviceScanActivity"
            android:label="可用设备" />

        <activity android:name=".driver.instruction.WriteCardActivity" />

        <activity android:name=".driver.instruction.IndicatorLampActivity" />

        <activity android:name=".driver.instruction.ZwInstructionActivity" />

        <activity android:name=".driver.instruction.CardInstructionActivity" />

        <activity android:name=".f6test.MainActivity" />

    </application>

</manifest>
